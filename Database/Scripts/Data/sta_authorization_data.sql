MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         101 AS ID,
         q'[Page 25: Role Privilege Overview]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         25 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         104 AS ID,
         q'[Navigation User Defined List: Jaarindeling (Administratie)]' AS NAME,
         q'[Jaarindeling]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         122 AS ID,
         q'[Navigation User Defined List: Leerjaren (Administratie)]' AS NAME,
         q'[Leerjaren]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         109 AS ID,
         q'[Page 2: Jaarindeling Overzicht - Interactive Grid: Jaarindeling Overzicht - Insert]' AS NAME,
         q'[Jaarindeling Overzicht]' AS APEX_COMPONENT_NAME,
         2 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[I]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         110 AS ID,
         q'[Page 2: Jaarindeling Overzicht - Interactive Grid: Jaarindeling Overzicht - Update]' AS NAME,
         q'[Jaarindeling Overzicht]' AS APEX_COMPONENT_NAME,
         2 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[U]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         121 AS ID,
         q'[Page 8: Administratie]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         8 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         123 AS ID,
         q'[Page 4: Leerjaren Overzicht]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         4 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         124 AS ID,
         q'[Page 4: Leerjaren Overzicht - Interactive Grid: Leerjaren Overzicht - Delete]' AS NAME,
         q'[Leerjaren Overzicht]' AS APEX_COMPONENT_NAME,
         4 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[D]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         125 AS ID,
         q'[Page 4: Leerjaren Overzicht - Interactive Grid: Leerjaren Overzicht - Insert]' AS NAME,
         q'[Leerjaren Overzicht]' AS APEX_COMPONENT_NAME,
         4 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[I]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         127 AS ID,
         q'[Navigation Hoofdmenu: Leerkrachten Overzicht]' AS NAME,
         q'[Leerkrachten Overzicht]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         141 AS ID,
         q'[Page 14: Leerkracht Toevoegen - Read Only]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         14 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         1 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         133 AS ID,
         q'[Page 14: Leerkracht Toevoegen - Button: DELETE]' AS NAME,
         q'[DELETE]' AS APEX_COMPONENT_NAME,
         14 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         82 AS ID,
         q'[Navigation User Defined List: Authorisatie (Administratie)]' AS NAME,
         q'[Authorisatie]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         163 AS ID,
         q'[Page 9: Students Overview - Button: CREATE]' AS NAME,
         q'[CREATE]' AS APEX_COMPONENT_NAME,
         9 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         164 AS ID,
         q'[Page 10: Add Student]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         165 AS ID,
         q'[Page 10: Add Student - Button: CREATE]' AS NAME,
         q'[CREATE]' AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         166 AS ID,
         q'[Page 10: Add Student - Button: SAVE]' AS NAME,
         q'[SAVE]' AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         167 AS ID,
         q'[Page 10: Add Student - Button: DELETE]' AS NAME,
         q'[DELETE]' AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         129 AS ID,
         q'[Page 14: Leerkracht Toevoegen]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         14 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         105 AS ID,
         q'[Page 2: Jaarindeling Overzicht]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         2 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         126 AS ID,
         q'[Page 4: Leerjaren Overzicht - Interactive Grid: Leerjaren Overzicht - Update]' AS NAME,
         q'[Leerjaren Overzicht]' AS APEX_COMPONENT_NAME,
         4 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[U]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         130 AS ID,
         q'[Page 7: Leerkrachten Overzicht - Button: CREATE]' AS NAME,
         q'[CREATE]' AS APEX_COMPONENT_NAME,
         7 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         132 AS ID,
         q'[Page 14: Leerkracht Toevoegen - Button: SAVE]' AS NAME,
         q'[SAVE]' AS APEX_COMPONENT_NAME,
         14 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         103 AS ID,
         q'[Navigation Hoofdmenu: Administratie]' AS NAME,
         q'[Administratie]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         102 AS ID,
         q'[Page 18: Klassen Overzicht - Interactive Grid: Klassen Overzicht - Insert]' AS NAME,
         q'[Klassen Overzicht]' AS APEX_COMPONENT_NAME,
         18 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[I]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         131 AS ID,
         q'[Page 14: Leerkracht Toevoegen - Button: CREATE]' AS NAME,
         q'[CREATE]' AS APEX_COMPONENT_NAME,
         14 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         111 AS ID,
         q'[Page 2: Jaarindeling Overzicht - Interactive Grid: Jaarindeling Overzicht - Delete]' AS NAME,
         q'[Jaarindeling Overzicht]' AS APEX_COMPONENT_NAME,
         2 AS APEX_PAGE,
         q'[IG]' AS APEX_COMPONENT_TYPE,
         q'[D]' AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         128 AS ID,
         q'[Page 7: Leerkrachten Overzicht]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         7 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         161 AS ID,
         q'[Navigation Hoofdmenu: Studenten Overzicht]' AS NAME,
         q'[Studenten Overzicht]' AS APEX_COMPONENT_NAME,
         null AS APEX_PAGE,
         q'[NAV]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         162 AS ID,
         q'[Page 9: Students Overview]' AS NAME,
         null AS APEX_COMPONENT_NAME,
         9 AS APEX_PAGE,
         q'[PAGE]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         168 AS ID,
         q'[Page 10: Add Student - Button: ADD_CARETAKER]' AS NAME,
         q'[ADD_CARETAKER]' AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

MERGE INTO STA_AUTHORIZATION a
USING
    (SELECT
         169 AS ID,
         q'[Page 10: Add Student - Button: EDIT_CARETAKER]' AS NAME,
         q'[EDIT_CARETAKER]' AS APEX_COMPONENT_NAME,
         10 AS APEX_PAGE,
         q'[BUTTONS]' AS APEX_COMPONENT_TYPE,
         null AS ROW_OPERATION,
         0 AS READ_ONLY
     FROM DUAL) b
ON ( a.ID = b.ID )
WHEN NOT MATCHED THEN
    INSERT (ID, NAME, APEX_COMPONENT_NAME, APEX_PAGE, APEX_COMPONENT_TYPE, ROW_OPERATION, READ_ONLY)
    VALUES (b.ID, b.NAME, b.APEX_COMPONENT_NAME, b.APEX_PAGE, b.APEX_COMPONENT_TYPE, b.ROW_OPERATION, b.READ_ONLY)
WHEN MATCHED THEN
    UPDATE SET
               a.NAME = b.NAME,
               a.APEX_COMPONENT_NAME = b.APEX_COMPONENT_NAME,
               a.APEX_PAGE = b.APEX_PAGE,
               a.APEX_COMPONENT_TYPE = b.APEX_COMPONENT_TYPE,
               a.ROW_OPERATION = b.ROW_OPERATION,
               a.READ_ONLY = b.READ_ONLY;

